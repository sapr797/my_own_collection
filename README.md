# Коллекция Ansible: `my_namespace.my_collection`
Коллекция, содержащая пользовательский модуль `my_own_module` для управления файлами, созданный в рамках обучения разработке под Ansible.

## 📦 Содержание коллекции
*   **Модули**:
    *   `my_own_module` — Создаёт, обновляет или удаляет файлы с заданным содержимым и правами.
*   **Роли**: *Пока не созданы (можно добавить позже)*
*   **Плагины**: *Пока не созданы (можно добавить позже)*

## 🚀 Быстрый старт
### Предварительные требования
*   **Ansible** версии 2.9 или выше (рекомендуется `ansible-core >= 2.15`).
*   **Python** 3.6 или выше.
*   Доступ к целевому хосту (для тестов можно использовать `localhost`).

### Установка коллекции
1.  **Установите коллекцию из локального пути** (режим разработки):
        ansible-galaxy collection install /путь/до/my_namespace/my_collection --force
    ```

2.  **Или установите из построенного tarball**:
    # 1. Соберите коллекцию
    cd my_namespace/my_collection
    ansible-galaxy collection build --output-path /tmp/
    # 2. Установите
    ansible-galaxy collection install /tmp/my_namespace-my_collection-1.0.0.tar.gz
    ```

## 📄 Использование модуля `my_own_module`
### Параметры модуля

| Параметр | Обязательный | Тип | Описание                                                                                         |
| :------- | :----------: | :--- | :----------------------------------------------------------------------------------------------- |
| `path`   |      Да      | str  | Абсолютный путь к файлу.                                                                         |
| `state`  |      Нет     | str  | Состояние файла. Варианты: `present` (по умолчанию), `absent`.                                   |
| `content`|      Нет     | str  | Содержимое файла. По умолчанию — пустая строка.                                                  |
| `mode`   |      Нет     | str  | Права доступа (в восьмеричном формате, например, `0644`).                                         |
| `owner`  |      Нет     | str  | Владелец файла (пользователь).                                                                   |
| `group`  |      Нет     | str  | Группа файла.                                                                                    |

### Примеры Playbook

#### Пример 1: Создание простого файла
```yaml
- name: Создать тестовый файл
  hosts: localhost
  tasks:
    - name: Создаём файл
      my_namespace.my_collection.my_own_module:
        path: /tmp/hello_world.txt
        content: |
          Привет, мир!
          Это тест моего модуля.
        mode: "0644"
        owner: "ubuntu"
        state: present
Пример 2: Удаление файла
yaml
- name: Удалить файл
  hosts: localhost
  tasks:
    - name: Удаляем файл
      my_namespace.my_collection.my_own_module:
        path: /tmp/old_file.txt
        state: absent
Пример 3: Обновление файла (идемпотентность)
При повторном запуске с теми же параметрами модуль сообщит "changed": false, если файл уже существует с идентичным содержимым и правами.

yaml
- name: Гарантировать наличие конфига
  hosts: webservers
  tasks:
    - name: Конфигурационный файл на месте
      my_namespace.my_collection.my_own_module:
        path: /etc/myapp/config.ini
        content: |
          [default]
          debug = no
          port = 8080
        mode: "0600"
        owner: "root"
Возвращаемые значения
Модуль возвращает JSON-объект с информацией о выполненном действии:

Ключ	Тип	Пример	Описание
changed	bool	true	Было ли внесено изменение в систему.
path	str	"/tmp/hello.txt"	Путь к целевому файлу.
state	str	"present"	Запрошенное состояние файла.
size	int	42	Размер файла в байтах (только при present).
checksum	str	"a1b2c3..."	SHA1 контрольная сумма файла.
🛠 Разработка и тестирование
Структура проекта
my_namespace/my_collection/
├── README.md                      # Этот файл
├── galaxy.yml                     # Метаданные коллекции
├── plugins/
│   └── modules/
│       └── my_own_module.py       # Исходный код модуля
├── meta/
│   └── runtime.yml                # Требования к окружению (опционально)
└── tests/
    └── test_my_module.yml         # Интеграционный тест
Локальное тестирование
Активируйте виртуальное окружение и установите зависимости:

python3 -m venv venv
source venv/bin/activate
pip install ansible-core
Установите коллекцию в режиме разработки:

ansible-galaxy collection install ./ --force
Запустите тестовый playbook:

ansible-playbook tests/test_my_module.yml --connection=local

Ожидаемый результат
При успешной проверке вы увидите:

✅ Создание файла → changed: true

✅ Идемпотентность → changed: false (при повторном запуске без изменений)

✅ Обновление содержимого → changed: true

✅ Изменение прав доступа → changed: true

✅ Удаление файла → changed: true

✅ Идемпотентность удаления → changed: false

✅ Создание с путём → changed: true

🔍 Дополнительные проверки
Проверка файла в системе:

bash
# Проверьте созданные файлы
ls -la /tmp/test_*.txt
ls -la /tmp/testdir/subdir/

# Проверьте содержимое
cat /tmp/testdir/subdir/final_test.txt
Проверка документации модуля:

bash
ansible-doc my_namespace.my_collection.my_own_module
📋 Чек-лист проверки работоспособности
Функция	Как проверить	Ожидаемый результат
Создание файла	Запуск плейбука	Файл создаётся с указанным содержимым
Идемпотентность	Повторный запуск	"changed": false
Обновление содержимого	Изменение параметра content	Содержимое файла обновляется
Изменение прав	Изменение параметра mode	Права доступа меняются
Удаление файла	state: absent	Файл удаляется из системы
Обработка путей	Создание файла во вложенной папке	Директории создаются автоматически
Возвращаемые значения	Просмотр register	Корректные path, changed, size

ansible-galaxy collection list | grep my_namespace
Проверьте виртуальное окружение:

which ansible-playbook
# Должен быть путь к venv: /home/ubuntu/my_ansible_project/venv/bin/ansible-playbook

### ✅ Тестирование
Модуль был протестирован на выполнение всех заявленных функций:

| Функция | Тестовый сценарий | Результат |
|---------|-------------------|-----------|
| **Создание файла** | Создание нового файла с содержимым | ✅ Успешно |
| **Идемпотентность** | Повторное выполнение без изменений | ✅ `changed: false` |
| **Обновление содержимого** | Изменение содержимого существующего файла | ✅ Успешно |
| **Изменение прав доступа** | Смена прав с 0644 на 0600 | ✅ Успешно |
| **Удаление файла** | Удаление созданного файла | ✅ Успешно |
| **Создание с путём** | Автоматическое создание вложенных директорий | ✅ Успешно |

📝 Лицензия
MIT© sapr797

🤝 Вклад в разработку
Предложения по улучшению и пул-реквесты приветствуются! 

Эта коллекция была создана в рамках изучения разработки модулей и коллекций Ansible.


#### Запуск полного теста
```bash
ansible-playbook complete_test.yml --connection=local
